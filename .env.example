# Convex / Better Auth production
NEXT_PUBLIC_CONVEX_URL="https://YOUR-DEPLOYMENT.convex.cloud"
NEXT_PUBLIC_CONVEX_SITE_URL="https://YOUR-DEPLOYMENT.convex.site"
CONVEX_DEPLOY_KEY="set in CI/Vercel only"
SITE_URL="https://YOUR-CRESTLINE-DOMAIN"
BETTER_AUTH_SECRET="generate a long random secret at least 32 characters"
ADMIN_BOOTSTRAP_SECRET="generate a separate long random secret"

# Payment provider secrets belong in the Convex production deployment.
PAYSTACK_SECRET_KEY="set in Convex production environment only"
