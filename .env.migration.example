# One-time migration environment. Do not add these to the application runtime.
MONGODB_URI="mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/crestline"
NEXT_PUBLIC_CONVEX_URL="https://YOUR-DEPLOYMENT.convex.cloud"
MIGRATION_SECRET="generate a separate long random secret and set the same value in Convex production"
