"use client";
import { useState } from "react";
import { Bell, CreditCard, Grid, Headphones, Home, PlusCircle, Send, Settings, TrendingUp, MessageCircle, ChevronDown } from "lucide-react";
const nav=[['Home','/dashboard',Home],['Transfer','/transfer',Send],['Deposit','/deposit',PlusCircle],['Activity','/transactions',TrendingUp],['Cards','/cards',CreditCard],['Settings','/settings',Settings],['Support','/support',Headphones],['More','/account',Grid]] as const;
export default function SupportPage(){const [q,setQ]=useState('');const [subject,setSubject]=useState('');const [message,setMessage]=useState('');const [tickets,setTickets]=useState<{subject:string;status:string;date:string}[]>([]);const [open,setOpen]=useState<number|null>(null);const faqs=[['How long do transfers take?','Internal member transfers are normally processed immediately. External transfers depend on the receiving institution.'],['How do I freeze my card?','Open Cards and use the Freeze/Unfreeze control for the relevant card.'],['Why is my KYC pending?','Your verification remains pending until the required identity checks are completed.']];const submit=()=>{if(!subject||!message)return;setTickets(t=>[{subject,status:'Open',date:new Date().toLocaleDateString()},...t]);setSubject('');setMessage('')};return <main className="min-h-screen bg-slate-50 pb-28 text-slate-900"><header className="border-b border-slate-100 bg-white px-5 py-4"><div className="mx-auto flex max-w-5xl items-center justify-between"><a href="/dashboard" className="flex items-center gap-3 font-bold"><span className="grid h-10 w-10 place-items-center rounded-full bg-sky-500 text-white">C</span>Crestline Capital</a><button className="rounded-full bg-slate-100 p-2"><Bell size={18}/></button></div></header><section className="mx-auto max-w-2xl px-5 py-7"><p className="text-xs font-bold tracking-[.18em] text-sky-500">CUSTOMER CARE</p><h1 className="mt-1 text-3xl font-bold">Support Center</h1><p className="mt-2 text-sm text-slate-500">Get help with transfers, cards, deposits and account security.</p><div className="mt-6 rounded-3xl bg-gradient-to-br from-sky-500 to-blue-600 p-6 text-white"><div className="flex items-center gap-3"><MessageCircle/><div><b>Live chat</b><p className="text-xs text-white/80">Connect with a support agent</p></div></div><button className="mt-5 rounded-xl bg-white px-4 py-2 text-sm font-semibold text-sky-600">Start chat</button></div><section className="mt-5 rounded-3xl border border-slate-100 bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Create a support ticket</h2><input value={subject} onChange={e=>setSubject(e.target.value)} placeholder="Subject" className="mt-4 w-full rounded-xl border border-slate-200 p-3 outline-none focus:border-sky-400"/><textarea value={message} onChange={e=>setMessage(e.target.value)} placeholder="Describe your issue" rows={4} className="mt-3 w-full rounded-xl border border-slate-200 p-3 outline-none focus:border-sky-400"/><button onClick={submit} className="mt-3 w-full rounded-xl bg-sky-500 py-3 font-semibold text-white">Submit ticket</button></section><section className="mt-5 rounded-3xl border border-slate-100 bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">My tickets</h2>{tickets.length===0?<p className="mt-3 text-sm text-slate-400">No support tickets yet.</p>:tickets.map((t,i)=><div key={i} className="mt-3 flex items-center justify-between rounded-xl bg-slate-50 p-3"><div><b className="text-sm">{t.subject}</b><p className="text-xs text-slate-400">{t.date}</p></div><span className="rounded-full bg-amber-50 px-3 py-1 text-xs font-semibold text-amber-600">{t.status}</span></div>)}</section><section className="mt-5 rounded-3xl border border-slate-100 bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">Frequently asked questions</h2>{faqs.filter(x=>x[0].toLowerCase().includes(q.toLowerCase())).map((f,i)=><div key={i} className="border-b border-slate-100 last:border-0"><button onClick={()=>setOpen(open===i?null:i)} className="flex w-full items-center justify-between py-4 text-left text-sm font-semibold">{f[0]}<ChevronDown size={17} className={open===i?'rotate-180':''}/></button>{open===i&&<p className="pb-4 text-sm text-slate-500">{f[1]}</p>}</div>)}<input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search FAQs" className="mt-4 w-full rounded-xl border border-slate-200 p-3 text-sm"/></section></section><nav className="fixed bottom-4 left-4 right-4 z-50 mx-auto max-w-5xl rounded-full border border-slate-100 bg-white px-2 py-2 shadow-xl"><div className="flex gap-1 overflow-x-auto">{nav.map(([label,href,Icon])=><a key={label} href={href} className={`flex min-w-[58px] flex-1 flex-col items-center gap-1 rounded-xl px-2 py-1 text-[10px] font-semibold ${label==='Support'?'bg-sky-500 text-white':'text-slate-500'}`}><Icon size={17}/>{label}</a>)}</div></nav></main>}
