"use client";
import { useEffect, useState } from "react";
import { Bell, CreditCard, Grid, Headphones, Home, PlusCircle, Send, Settings as SettingsIcon, TrendingUp, ShieldCheck, Moon } from "lucide-react";
import { api } from "@/lib/api";
const nav=[['Home','/dashboard',Home],['Transfer','/transfer',Send],['Deposit','/deposit',PlusCircle],['Activity','/transactions',TrendingUp],['Cards','/cards',CreditCard],['Settings','/settings',SettingsIcon],['Support','/support',Headphones],['More','/account',Grid]] as const;
export default function SettingsPage(){const [user,setUser]=useState<any>(null);const [dark,setDark]=useState(false);const [twoFA,setTwoFA]=useState(false);const [currency,setCurrency]=useState('USD');const [saved,setSaved]=useState(false);useEffect(()=>{api.get('/user').then(r=>setUser(r.data.user)).catch(()=>{})},[]);const save=()=>{setSaved(true);setTimeout(()=>setSaved(false),2500)};return <main className={dark?'min-h-screen bg-slate-950 pb-28 text-white':'min-h-screen bg-slate-50 pb-28 text-slate-900'}><header className="border-b border-slate-100 bg-white px-5 py-4"><div className="mx-auto flex max-w-5xl items-center justify-between"><a href="/dashboard" className="flex items-center gap-3 font-bold"><span className="grid h-10 w-10 place-items-center rounded-full bg-sky-500 text-white">C</span>Crestline Capital</a><div className="flex gap-2"><button onClick={()=>setDark(!dark)} className="rounded-full bg-slate-100 p-2"><Moon size={18}/></button><button className="rounded-full bg-slate-100 p-2"><Bell size={18}/></button></div></div></header><section className="mx-auto max-w-2xl px-5 py-7"><p className="text-xs font-bold tracking-[.18em] text-sky-500">ACCOUNT SETTINGS</p><h1 className="mt-1 text-3xl font-bold">Settings</h1><div className="mt-6 space-y-4"><Panel title="Profile"><div className="grid gap-4 sm:grid-cols-2"><Field label="Full name" value={user?.name||''}/><Field label="Email" value={user?.email||''}/><Field label="Account number" value={user?.accountNumber||''}/><div><label className="text-xs font-semibold text-slate-500">KYC status</label><div className="mt-2 rounded-xl bg-emerald-50 px-4 py-3 text-sm font-semibold text-emerald-700">{user?.kycStatus||'Pending'}</div></div></div></Panel><Panel title="Security"><div className="flex items-center gap-3 rounded-2xl bg-slate-50 p-4"><ShieldCheck className="text-emerald-500"/><div className="flex-1"><b className="block text-sm">Two-factor authentication</b><span className="text-xs text-slate-500">Require an additional verification step at sign in.</span></div><button onClick={()=>setTwoFA(!twoFA)} className={`h-7 w-12 rounded-full p-1 ${twoFA?'bg-sky-500':'bg-slate-300'}`}><span className={`block h-5 w-5 rounded-full bg-white transition ${twoFA?'translate-x-5':''}`}/></button></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><button className="rounded-2xl border border-slate-200 px-4 py-3 text-sm font-semibold">Update password</button><button className="rounded-2xl border border-slate-200 px-4 py-3 text-sm font-semibold">Reset transaction PIN</button></div></Panel><Panel title="Preferences"><div className="grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">Theme<select value={dark?'dark':'light'} onChange={e=>setDark(e.target.value==='dark')} className="mt-2 w-full rounded-xl border border-slate-200 bg-white p-3 font-normal"><option value="light">Light</option><option value="dark">Dark</option></select></label><label className="text-sm font-semibold">Primary currency<select value={currency} onChange={e=>setCurrency(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white p-3 font-normal"><option>USD</option><option>EUR</option><option>GBP</option></select></label></div><button onClick={save} className="mt-4 rounded-2xl bg-sky-500 px-6 py-3 font-semibold text-white">Save preferences</button>{saved&&<span className="ml-3 text-sm text-emerald-600">Saved</span>}</Panel></div></section><nav className="fixed bottom-4 left-4 right-4 z-50 mx-auto max-w-5xl rounded-full border border-slate-100 bg-white px-2 py-2 shadow-xl"><div className="flex gap-1 overflow-x-auto">{nav.map(([label,href,Icon])=><a key={label} href={href} className={`flex min-w-[58px] flex-1 flex-col items-center gap-1 rounded-xl px-2 py-1 text-[10px] font-semibold ${label==='Settings'?'bg-sky-500 text-white':'text-slate-500'}`}><Icon size={17}/>{label}</a>)}</div></nav></main>}
function Panel({title,children}:{title:string;children:React.ReactNode}){return <section className="rounded-3xl border border-slate-100 bg-white p-5 shadow-sm"><h2 className="mb-4 text-lg font-bold">{title}</h2>{children}</section>}function Field({label,value}:{label:string;value:string}){return <label className="text-xs font-semibold text-slate-500">{label}<input value={value} readOnly className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm text-slate-700 outline-none"/></label>}
