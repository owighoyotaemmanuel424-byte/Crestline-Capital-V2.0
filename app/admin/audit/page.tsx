"use client";
import { useQuery } from "convex/react";
import { api } from "@/convex/_generated/api";
export default function AdminAuditPage(){const logs=useQuery(api.adminUsers.audit);return <div className="min-h-screen bg-slate-50 p-4 text-slate-950 sm:p-6 lg:p-8"><div className="mx-auto max-w-7xl"><div className="mb-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-blue-700">Site & System</p><h1 className="mt-2 text-3xl font-bold">Audit log</h1><p className="mt-1 text-sm text-slate-500">Immutable operational history for administrator and customer actions.</p></div><div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="w-full min-w-[900px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th className="px-5 py-3">When</th><th className="px-5 py-3">Action</th><th className="px-5 py-3">Actor</th><th className="px-5 py-3">Target</th><th className="px-5 py-3">Metadata</th></tr></thead><tbody className="divide-y divide-slate-100">{logs===undefined?Array.from({length:8}).map((_,i)=><tr key={i}><td colSpan={5} className="px-5 py-4"><div className="h-5 animate-pulse rounded bg-slate-100"/></td></tr>):logs.map(l=><tr key={l._id}><td className="px-5 py-4 text-slate-500">{new Date(l.createdAt).toLocaleString()}</td><td className="px-5 py-4 font-semibold">{l.action}</td><td className="px-5 py-4">{l.actor?.email||"System"}</td><td className="px-5 py-4 text-slate-500">{l.targetId||"—"}</td><td className="max-w-[420px] truncate px-5 py-4 font-mono text-xs text-slate-500">{JSON.stringify(l.metadata||{})}</td></tr>)}</tbody></table>{logs!==undefined&&!logs.length&&<div className="p-12 text-center text-sm text-slate-500">No audit events recorded.</div>}</div></div></div>}
