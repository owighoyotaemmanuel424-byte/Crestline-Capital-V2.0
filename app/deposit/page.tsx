"use client";
import { useState } from "react";
import { Bell, ChevronDown, CreditCard, Home, PlusCircle, Send, Settings, Headphones, Grid, TrendingUp, CheckCircle2, Building2, WalletCards, X } from "lucide-react";

const nav=[['Home','/dashboard',Home],['Transfer','/transfer',Send],['Deposit','/deposit',PlusCircle],['Activity','/transactions',TrendingUp],['Cards','/cards',CreditCard],['Settings','/settings',Settings],['Support','/support',Headphones],['More','/account',Grid]] as const;
export default function DepositPage(){
 const [method,setMethod]=useState<'bank'|'card'|'wire'>('bank'); const [amount,setAmount]=useState(''); const [open,setOpen]=useState(false); const [done,setDone]=useState(false);
 const confirm=()=>{if(Number(amount)>0)setOpen(true)};
 const receipt=()=>{const text=`Crestline Capital Deposit Receipt\nMethod: ${method}\nAmount: $${Number(amount).toFixed(2)}\nStatus: Confirmed\nDate: ${new Date().toLocaleString()}`;const blob=new Blob([text],{type:'text/plain'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download='crestline-deposit-receipt.txt';a.click();URL.revokeObjectURL(url)};
 return <main className="min-h-screen bg-slate-50 pb-28 text-slate-900"><header className="sticky top-0 z-40 border-b border-slate-100 bg-white/95 px-5 py-4 backdrop-blur"><div className="mx-auto flex max-w-5xl items-center justify-between"><a href="/dashboard" className="flex items-center gap-3 font-bold"><span className="grid h-10 w-10 place-items-center rounded-full bg-sky-500 text-white">C</span>Crestline Capital</a><div className="flex gap-2"><button className="rounded-full bg-slate-100 p-2"><span>◐</span></button><button className="relative rounded-full bg-slate-100 p-2"><Bell size={18}/><span className="absolute -right-0.5 -top-0.5 h-2 w-2 rounded-full bg-sky-500"/></button></div></div></header><section className="mx-auto max-w-2xl px-5 py-7"><p className="text-xs font-bold tracking-[.18em] text-sky-500">FUND YOUR ACCOUNT</p><h1 className="mt-1 text-3xl font-bold">Make a Deposit</h1><p className="mt-2 text-sm text-slate-500">Choose a funding method and follow the secure confirmation steps.</p><div className="mt-6 grid gap-3 sm:grid-cols-3">{[['bank','Bank Transfer',Building2],['card','Credit / Debit Card',WalletCards],['wire','Wire Transfer',Send]].map(([id,label,Icon])=><button key={id as string} onClick={()=>setMethod(id as any)} className={`rounded-2xl border p-4 text-left ${method===id?'border-sky-400 bg-sky-50 ring-2 ring-sky-100':'border-slate-100 bg-white'}`}><Icon size={22} className={method===id?'text-sky-500':'text-slate-500'}/><b className="mt-3 block text-sm">{label as string}</b><span className="mt-1 block text-xs text-slate-400">Secure funding</span></button>)}</div><div className="mt-5 rounded-3xl border border-slate-100 bg-white p-5 shadow-sm"><label className="text-sm font-semibold">Deposit amount</label><div className="mt-2 flex items-center rounded-2xl border border-slate-200 p-4"><span className="mr-3 font-bold text-slate-400">US$</span><input value={amount} onChange={e=>setAmount(e.target.value.replace(/[^0-9.]/g,''))} inputMode="decimal" placeholder="0.00" className="w-full text-3xl font-bold outline-none"/></div><div className="mt-4 grid grid-cols-4 gap-2">{['100','500','1000','5000'].map(v=><button key={v} onClick={()=>setAmount(v)} className="rounded-xl bg-slate-100 py-2 text-xs font-semibold">${Number(v).toLocaleString()}</button>)}</div><div className="mt-5 rounded-2xl bg-slate-50 p-4 text-sm"><div className="flex justify-between"><span className="text-slate-500">Funding method</span><b className="capitalize">{method}</b></div><div className="mt-2 flex justify-between"><span className="text-slate-500">Processing</span><b>Secure</b></div></div><button onClick={confirm} disabled={!Number(amount)} className="mt-5 w-full rounded-2xl bg-sky-500 py-3.5 font-semibold text-white disabled:opacity-50">Continue</button></div></section><nav className="fixed bottom-4 left-4 right-4 z-50 mx-auto max-w-5xl rounded-full border border-slate-100 bg-white px-2 py-2 shadow-xl"><div className="flex gap-1 overflow-x-auto">{nav.map(([label,href,Icon])=><a key={label} href={href} className={`flex min-w-[58px] flex-1 flex-col items-center gap-1 rounded-xl px-2 py-1 text-[10px] font-semibold ${label==='Deposit'?'bg-sky-500 text-white':'text-slate-500'}`}><Icon size={17}/>{label}</a>)}</div></nav>{open&&<div className="fixed inset-0 z-[60] grid place-items-center bg-slate-950/50 p-5"><div className="w-full max-w-md rounded-3xl bg-white p-6"><button onClick={()=>setOpen(false)} className="float-right"><X/></button><CheckCircle2 className="text-emerald-500" size={42}/><h2 className="mt-4 text-xl font-bold">Confirm deposit</h2><p className="mt-2 text-sm text-slate-500">You are about to start a ${Number(amount).toFixed(2)} {method} deposit. Continue to the provider and complete verification.</p><div className="mt-5 flex gap-2"><button onClick={()=>{setOpen(false);setDone(true)}} className="flex-1 rounded-2xl bg-sky-500 py-3 font-semibold text-white">Confirm</button><button onClick={()=>setOpen(false)} className="rounded-2xl bg-slate-100 px-5">Cancel</button></div></div></div>}{done&&<div className="fixed bottom-24 left-5 right-5 z-50 mx-auto max-w-md rounded-2xl bg-emerald-600 p-4 text-white shadow-xl"><div className="flex items-center justify-between"><span>Deposit request recorded successfully.</span><button onClick={receipt} className="rounded-lg bg-white/20 px-3 py-2 text-xs font-bold">Receipt</button></div></div>}</main>}
